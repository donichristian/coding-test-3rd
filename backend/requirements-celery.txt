# Celery worker dependencies (includes ML libraries)
-r requirements-common.txt

# Docling - Primary document processing
docling>=2.61.1,<3.0.0
docling-core>=2.29.0

# LangChain Docling integration
langchain-docling>=0.1.0

# PyTorch with CUDA support for Docling models (if available)
torch==2.2.2 --index-url https://download.pytorch.org/whl/cu121
torchvision==0.17.2 --index-url https://download.pytorch.org/whl/cu121

# Headless OpenCV for containerized environments
opencv-python-headless==4.11.0.86

# LLM and Embeddings - Focused on supported providers
openai>=1.6.1,<2.0.0
google-genai>=0.8.0
anthropic==0.8.1

# LangChain core for RAG pipeline
langchain>=0.1.0
langchain-core>=0.2.0
langchain-openai>=0.1.0
langchain-community>=0.0.20
langchain-google-genai>=1.0.0

# Embeddings - sentence-transformers for local processing
sentence-transformers>=2.7.0

# Testing (only for development)
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0
anyio==4.11.0

# Development (only for development)
black==23.12.1
flake8==7.0.0
mypy==1.8.0